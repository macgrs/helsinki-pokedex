name: 11.6-Hello World and Date!

on:
  push:
    branches:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
      - name: List files
        run: |
          ls
      - name: Get Date
        run: |
          date
          
